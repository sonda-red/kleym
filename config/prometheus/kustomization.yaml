resources:
- monitor.yaml

# The following patch configures the ServiceMonitor to use the metrics-server-cert Secret.
# Enable the matching cert_metrics_manager_patch.yaml patch in config/default/kustomization.yaml
# when deploying the manager with that Secret mounted.
#patches:
#  - path: monitor_tls_patch.yaml
#    target:
#      kind: ServiceMonitor

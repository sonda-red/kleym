---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: inferencetrustbindings.kleym.sonda.red
spec:
  group: kleym.sonda.red
  names:
    kind: InferenceTrustBinding
    listKind: InferenceTrustBindingList
    plural: inferencetrustbindings
    singular: inferencetrustbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceTrustBinding is the Schema for the inferencetrustbindings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of InferenceTrustBinding
            properties:
              attributionLog:
                description: |-
                  attributionLog configures audit logging for identity attribution.
                  Logs include caller identity, workload SPIFFE ID, and request metadata.
                properties:
                  enabled:
                    default: true
                    description: enabled turns attribution logging on or off
                    type: boolean
                  format:
                    default: json
                    description: |-
                      format specifies the log output format
                      - "json": Structured JSON logs (recommended for SIEM integration)
                      - "text": Human-readable text format
                    enum:
                    - json
                    - text
                    type: string
                  includeRequestMetadata:
                    default: false
                    description: |-
                      includeRequestMetadata adds request-level metadata (method, path, headers) to logs.
                      Note: This is deployment-level logging only. Per-request audit trails are out of MVP scope.
                    type: boolean
                type: object
              mtlsRequired:
                default: true
                description: |-
                  mtlsRequired enforces mutual TLS for all traffic to matched inference workloads.
                  When true, kleym ensures SPIRE-issued SVIDs are available and configures
                  mTLS enforcement (e.g., via sidecar injection or native SPIFFE support).
                type: boolean
              policyRef:
                description: |-
                  policyRef is an optional reference to an external policy resource (e.g., OPA ConfigMap)
                  that governs access control decisions for the matched workloads.
                  Policy integration is optional and pluggable—kleym does not implement policy engines.
                properties:
                  kind:
                    description: kind is the type of policy resource (e.g., "ConfigMap",
                      "OPAPolicy")
                    minLength: 1
                    type: string
                  name:
                    description: name is the name of the policy resource
                    minLength: 1
                    type: string
                  namespace:
                    description: namespace is the namespace of the policy resource
                      (defaults to binding namespace)
                    type: string
                required:
                - kind
                - name
                type: object
              selector:
                description: |-
                  selector identifies which inference workloads this binding applies to.
                  Uses standard Kubernetes label selectors to match Pods or Deployments.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              spiffeIdScope:
                default: pod
                description: |-
                  spiffeIDScope determines the granularity of SPIFFE identity assignment.
                  - "pod": Each pod receives a unique SPIFFE ID (recommended for audit granularity)
                  - "replicaSet": All pods in a ReplicaSet share one SPIFFE ID
                  - "deployment": All pods in a Deployment share one SPIFFE ID
                enum:
                - pod
                - replicaSet
                - deployment
                type: string
            required:
            - selector
            type: object
          status:
            description: status defines the observed state of InferenceTrustBinding
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the InferenceTrustBinding resource.
                  Condition types:
                  - "Ready": Binding is active and identities are being issued
                  - "SPIREConnected": SPIRE integration is healthy
                  - "PolicyResolved": Referenced policy (if any) is available
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              identitiesIssued:
                description: identitiesIssued is the count of SPIFFE identities issued
                  for matched workloads
                format: int32
                type: integer
              matchedWorkloads:
                description: matchedWorkloads is the count of inference workloads
                  currently matched by this profile
                format: int32
                type: integer
              mtlsEnforced:
                description: mtlsEnforced indicates whether mTLS is currently enforced
                  for all matched workloads
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: inferenceidentitybindings.kleym.sonda.red
spec:
  group: kleym.sonda.red
  names:
    kind: InferenceIdentityBinding
    listKind: InferenceIdentityBindingList
    plural: inferenceidentitybindings
    singular: inferenceidentitybinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.poolRef.name
      name: POOL
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: REASON
      type: string
    - jsonPath: .status.computedSpiffeIDs[0].spiffeID
      name: SPIFFE ID
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceIdentityBinding is the Schema for the inferenceidentitybindings
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of InferenceIdentityBinding.
            properties:
              poolRef:
                description: poolRef specifies the required InferencePool workload
                  anchor in the same namespace.
                properties:
                  group:
                    description: |-
                      group optionally constrains pool resolution to the supported GAIE API group.
                      Omit this to use the served supported InferencePool group.
                    enum:
                    - inference.networking.k8s.io
                    type: string
                  name:
                    description: name of the InferencePool resource to bind to.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              serviceAccountName:
                description: serviceAccountName is the Kubernetes service account
                  required in every rendered identity selector set.
                maxLength: 253
                minLength: 1
                type: string
            required:
            - poolRef
            - serviceAccountName
            type: object
          status:
            description: status defines the observed state of InferenceIdentityBinding.
            properties:
              clusterSPIFFEIDClassName:
                description: |-
                  clusterSPIFFEIDClassName is the operator ClusterSPIFFEID class name used
                  when rendering the latest status. Empty means classless ClusterSPIFFEID output.
                type: string
              computedSpiffeIDs:
                description: computedSpiffeIDs lists the SPIFFE IDs produced from
                  the binding references.
                items:
                  description: ComputedSpiffeIDStatus holds a single computed SPIFFE
                    ID.
                  properties:
                    spiffeID:
                      description: spiffeID is the computed SPIFFE ID for this binding.
                      type: string
                  required:
                  - spiffeID
                  type: object
                type: array
              conditions:
                description: |-
                  conditions represent the latest available observations of the binding's state.

                  Known condition types:
                    - "Ready"
                    - "InvalidRef"
                    - "UnsafeSelector"
                    - "RenderFailure"
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              renderedSelectors:
                description: renderedSelectors shows the final workload selectors
                  for each rendered identity.
                items:
                  description: RenderedSelectorStatus describes the final selectors
                    for one rendered identity.
                  properties:
                    selectors:
                      description: selectors are the final set of workload selectors
                        applied to this identity.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                    spiffeID:
                      description: spiffeID is the computed SPIFFE ID for this rendered
                        identity.
                      type: string
                  required:
                  - spiffeID
                  type: object
                type: array
              trustDomain:
                description: trustDomain is the operator trust domain used when rendering
                  the latest status.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: inferenceidentitybindings.kleym.sonda.red
spec:
  group: kleym.sonda.red
  names:
    kind: InferenceIdentityBinding
    listKind: InferenceIdentityBindingList
    plural: inferenceidentitybindings
    singular: inferenceidentitybinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceIdentityBinding is the Schema for the inferenceidentitybindings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of InferenceIdentityBinding
            properties:
              containerDiscriminator:
                description: |-
                  containerDiscriminator specifies how to discriminate between containers when generating identities in PerObjective mode.
                  Required if mode is PerObjective, must be empty if mode is PoolOnly.
                properties:
                  type:
                    description: Type specifies the type of discriminator to use (e.g.,
                      ContainerName, ContainerImage).
                    type: string
                  value:
                    description: value is the container name or image to use as a
                      discriminator, depending on the Type.
                    type: string
                required:
                - type
                - value
                type: object
              mode:
                enum:
                - PoolOnly
                - PerObjective
                default: PerObjective
                description: mode selects the identity granularity for this binding.
                type: string
              selectorSource:
                description: selectorSource defines how the workload selectors are
                  derived.
                enum:
                - DerivedFromPool
                type: string
              spiffeIDTemplate:
                description: spiffeIDTemplate optionally overrides the SPIFFE ID template
                  for this binding. If not specified, the default template from the
                  controller configuration will be used.
                type: string
              targetRef:
                description: TargetRef specifies the reference to the InferenceObjective
                  in the same namespace
                properties:
                  name:
                    description: Name of the InferenceObjective resource to bind to.
                    type: string
                required:
                - name
                type: object
              workloadSelectorTemplates:
                description: workloadSelectorTemplates are required safety templates
                  that define how to derive workload selectors for this binding.
                items:
                  type: string
                minItems: 1
                type: array
                x-kubernetes-list-type: set
            required:
            - selectorSource
            - targetRef
            - workloadSelectorTemplates
            type: object
          status:
            description: status defines the observed state of InferenceIdentityBinding
            properties:
              computedSpiffeID:
                description: computedSpiffeID is the computed SPIFFE ID for this binding,
                  based on the SpiffeIDTemplate and the TargetRef.
                items:
                  properties:
                    mode:
                      description: mode indicates the mode of SPIFFE ID generation
                        (e.g., PoolOnly, PerObjective).
                      type: string
                    spiffeID:
                      description: spiffeID is the computed SPIFFE ID for this binding
                      type: string
                  required:
                  - spiffeID
                  type: object
                type: array
              conditions:
                description: |-
                  conditions represent the current state of the InferenceIdentityBinding resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              renderedSelectors:
                description: renderedSelectors shows final selectors applied to rendered
                  identities, after processing the WorkloadSelectorTemplates and SelectorSource.
                items:
                  description: renderedSelectorStatus describes final selectors for
                    one rendered identity, after processing the WorkloadSelectorTemplates
                    and SelectorSource.
                  properties:
                    selectors:
                      description: selectors are the final set of selectors that will
                        be applied to workloads matching this binding.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                    spiffeID:
                      description: spiffeID is the computed SPIFFE ID for this binding
                      type: string
                  required:
                  - spiffeID
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

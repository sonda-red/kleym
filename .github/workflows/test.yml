name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    name: Run on arc-nuc-set
    runs-on: arc-nuc-set
    container:
      image: golang:1.23
    steps:
      - name: Clone the code
        uses: actions/checkout@v4

      - name: Running Tests
        run: |
          go mod tidy
          make test

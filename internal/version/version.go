package version

// Version is the kleym version reported by released binaries and operator logs.
// Local builds keep the default development value unless overridden at link time.
var Version = "dev"
